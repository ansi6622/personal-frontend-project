import React from "react";

const Header = () =>{

  const floatLeft = { float: "left" };
  return(
    <header style={floatLeft}>
      <h1>Cash Register</h1>
    </header>
  )
};
export default Header;